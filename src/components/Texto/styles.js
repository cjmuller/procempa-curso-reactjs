import styled, {css} from 'styled-components';

const BaseTexto = styled.input`
    font-weight: bold;
    padding: 8px;
    width: 280px;
    border-radius: 4px;
    display: flex;
    flex-direction: column;
    margin: 10px 0;
`;

export default BaseTexto;
